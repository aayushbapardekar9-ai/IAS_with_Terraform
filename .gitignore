# Local.terraform directories
# These contain large binary plugins (hundreds of MBs) specific to your OS.
**/.terraform/*

#.tfstate files
# CRITICAL: These contain your running infrastructure state and often secrets.
# Never commit these to version control.
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all.tfvars files
# These usually contain sensitive input variables (like DB passwords).
*.tfvars
*.tfvars.json

# Ignore override files
# These are used for local overrides and shouldn't be shared.
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc
